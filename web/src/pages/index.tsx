import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="flex flex-col mt-16">
      <h1 className="text-5xl font-medium font-gtw">A stylized command menu for React</h1>
    </div>
  );
};

export default Home;