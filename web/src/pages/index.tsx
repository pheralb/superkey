import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="flex flex-col items-center justify-center mt-16 bg-gradient-to-r from-gray-100 to-gray-300">
      <h1 className="text-5xl font-medium">A stylized command menu for React</h1>
    </div>
  );
};

export default Home;